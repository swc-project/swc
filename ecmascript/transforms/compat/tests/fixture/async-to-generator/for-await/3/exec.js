
{
    function test() {
        const hasDataInReplicache = async (replicache) => {
            for await (const _ of [1, 3, 5, 7, 9]) {
                return true
            }
            return false
        }

        console.log(hasDataInReplicache())
        console.log(hasDataInReplicache())

    }

    test()
    test()
}

{
    function test() {
        const hasDataInReplicache = async (replicache) => {
            for await (const _ of [1, 3, 5, 7, 9]) {
                return true
            }
            return false
        }

        console.log(hasDataInReplicache())
        console.log(hasDataInReplicache())

    }

    test()
    test()
}

{
    function test() {
        const hasDataInReplicache = async (replicache) => {
            for await (const _ of [1, 3, 5, 7, 9]) {
                return true
            }
            return false
        }

        console.log(hasDataInReplicache())
        console.log(hasDataInReplicache())

    }

    test()
    test()
}

{
    function test() {
        const hasDataInReplicache = async (replicache) => {
            for await (const _ of [1, 3, 5, 7, 9]) {
                return true
            }
            return false
        }

        console.log(hasDataInReplicache())
        console.log(hasDataInReplicache())

    }

    test()
    test()
}

{
    function test() {
        const hasDataInReplicache = async (replicache) => {
            for await (const _ of [1, 3, 5, 7, 9]) {
                return true
            }
            return false
        }

        console.log(hasDataInReplicache())
        console.log(hasDataInReplicache())

    }

    test()
    test()
}

{
    function test() {
        const hasDataInReplicache = async (replicache) => {
            for await (const _ of [1, 3, 5, 7, 9]) {
                return true
            }
            return false
        }

        console.log(hasDataInReplicache())
        console.log(hasDataInReplicache())

    }

    test()
    test()
}

{
    function test() {
        const hasDataInReplicache = async (replicache) => {
            for await (const _ of [1, 3, 5, 7, 9]) {
                return true
            }
            return false
        }

        console.log(hasDataInReplicache())
        console.log(hasDataInReplicache())

    }

    test()
    test()
}

{
    function test() {
        const hasDataInReplicache = async (replicache) => {
            for await (const _ of [1, 3, 5, 7, 9]) {
                return true
            }
            return false
        }

        console.log(hasDataInReplicache())
        console.log(hasDataInReplicache())

    }

    test()
    test()
}

{
    function test() {
        const hasDataInReplicache = async (replicache) => {
            for await (const _ of [1, 3, 5, 7, 9]) {
                return true
            }
            return false
        }

        console.log(hasDataInReplicache())
        console.log(hasDataInReplicache())

    }

    test()
    test()
}

{
    function test() {
        const hasDataInReplicache = async (replicache) => {
            for await (const _ of [1, 3, 5, 7, 9]) {
                return true
            }
            return false
        }

        console.log(hasDataInReplicache())
        console.log(hasDataInReplicache())

    }

    test()
    test()
}

{
    function test() {
        const hasDataInReplicache = async (replicache) => {
            for await (const _ of [1, 3, 5, 7, 9]) {
                return true
            }
            return false
        }

        console.log(hasDataInReplicache())
        console.log(hasDataInReplicache())

    }

    test()
    test()
}

{
    function test() {
        const hasDataInReplicache = async (replicache) => {
            for await (const _ of [1, 3, 5, 7, 9]) {
                return true
            }
            return false
        }

        console.log(hasDataInReplicache())
        console.log(hasDataInReplicache())

    }

    test()
    test()
}

{
    function test() {
        const hasDataInReplicache = async (replicache) => {
            for await (const _ of [1, 3, 5, 7, 9]) {
                return true
            }
            return false
        }

        console.log(hasDataInReplicache())
        console.log(hasDataInReplicache())

    }

    test()
    test()
}