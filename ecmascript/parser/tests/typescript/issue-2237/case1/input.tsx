import React from "react";

export function Results() {
    return (
        <div>
            {[].map(() => {
                function onClick(): void {}

                return null;
            })}
        </div>
    );
}
