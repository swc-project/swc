[package]
edition = "2018"
name = "plugin_ast"
version = "0.1.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
abi_stable = "0.10.3"
num-bigint = "0.2"
rplugin = {path = "../../rplugin/"}
swc_atoms = {path = "../../atoms/"}
swc_common = {path = "../../common/", features = ["plugin-base"]}
swc_ecma_ast = {path = "../ast/"}

[dev-dependencies]
serde = "1"
serde_json = "1"
swc_ecma_parser = {path = "../parser/"}
testing = {version = "0.15.1", path = "../../testing"}
