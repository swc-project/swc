var a ;
if (// if undeclared it's assumed to have side-effects
if (b()) {
    // if undeclared it's assumed to have side-effects
if (b()) {
    a(// if undeclared it's assumed to have side-effects
if (b()) {
    a(c);
} else {
    // if undeclared it's assumed to have side-effects
if (b()) {
    a(c);
} else {
    a(// if undeclared it's assumed to have side-effects
if (b()) {
    a(c);
} else {
    a(d);
}
