interface:
  display_name: "Add Changeset"
  short_description: "Create, commit, and push SWC changesets"
  default_prompt: "Use $add-changeset to add, commit, and push a complete SWC changeset for the current PR."
