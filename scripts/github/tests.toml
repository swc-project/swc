[os.mac]
swc_plugin_runner = true

[os.windows]
swc = true
swc_cli = true
swc_common = true
swc_css_codegen = true
swc_css_parser = true
swc_ecma_codegen = true
swc_ecma_loader = true
swc_ecma_parser = true
swc_ecma_preset_env = true
swc_ecma_transforms_module = true
swc_ecma_transforms_testing = true
swc_node_bundler = true
swc_plugin = true
swc_plugin_runner = true
swc_plugin_testing = true

[check]
node = [
  "check",
  "check --no-default-features --features swc_v2",
]
swc = [
  "hack check --feature-powerset --no-dev-deps",
]
swc_bundler = [
  "hack check --feature-powerset --no-dev-deps",
]
swc_common = [
  "hack check --feature-powerset --no-dev-deps",
]
swc_ecma_ast = [
  "hack check --feature-powerset --no-dev-deps",
]
swc_ecma_loader = [
  "hack check --feature-powerset --no-dev-deps",
]
swc_ecma_transforms = [
  "hack check --feature-powerset --no-dev-deps",
]
swc_ecma_transforms_base = [
  "hack check --feature-powerset --no-dev-deps",
]
swc_ecma_transforms_compat = [
  "hack check --feature-powerset --no-dev-deps",
]
swc_ecma_transforms_optimization = [
  "hack check --feature-powerset --no-dev-deps",
]
swc_ecma_utils = [
  "hack check --feature-powerset --no-dev-deps",
]
swc_node_bundler = [
  "check",
  "check --no-default-features --features swc_v2",
]
