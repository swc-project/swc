let x: number extends string extends string ? string : string;
let x: number extends string number extends string ? string : string;
let x: number extends infer T extends number ? number : number ? number : number;
