function leading() {
    let x = 1;

    /* Leading on update */ ++ /* Leading on arg */ x /* Trailing on arg */;
}

function trailing() {
    let x = 1;

    /* Leading on arg */ x /* Trailing on arg */ ++ /* Trailing on update */;
}
