var strOrNum, numOrBool, str, num, bool;
switch(strOrNum){
    case strOrNum:
        break;
    case str:
    case num:
        break;
    case numOrBool:
        break;
    case bool:
        break;
}
