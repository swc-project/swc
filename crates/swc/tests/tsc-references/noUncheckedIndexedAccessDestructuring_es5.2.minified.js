import * as swcHelpers from "@swc/helpers";
swcHelpers.slicedToArray(strArray, 1)[0].toString(), swcHelpers.toArray(strArray).slice(0).push(void 0), swcHelpers.toArray(strArray).slice(2).push(void 0), strMap.t1.toString(), swcHelpers.extends({}, strMap).z.toString();
var ref, x = numMapPoint.x, y = numMapPoint.y, z = numMapPoint.z;
x.toFixed(), y.toFixed(), z.toFixed();
var x1 = numMapPoint.x, q = swcHelpers.objectWithoutProperties(numMapPoint, [
    "x"
]);
x1.toFixed(), q.y.toFixed(), q.z.toFixed(), target_string = swcHelpers.slicedToArray(strArray, 1)[0], target_string_undef = swcHelpers.slicedToArray(strArray, 1)[0], target_string_arr = swcHelpers.toArray(strArray).slice(3), (ref = numMapPoint).x, ref.y, ref.z, numMapPoint.q;
