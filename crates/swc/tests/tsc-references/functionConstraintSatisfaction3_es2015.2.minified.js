var i, c, i2, c2;
function foo(x) {
    return x;
}
foo((x)=>x
), foo((x)=>x
), foo(function(x) {
    return x;
}), foo(function(x) {
    return x;
}), foo(i), foo(c), foo(function(x) {
    return x;
}), foo((x)=>x
), foo(i2), foo(c2);
