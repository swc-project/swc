for (const comp of [])for (const item of (comp.sp.y = comp.sp.r.find((k)=>k.c == (comp.xp ? '1' : '0')), comp.c)){
    var ref;
    item.v = !!(null === (ref = item.t) || void 0 === ref ? void 0 : ref.length);
}
