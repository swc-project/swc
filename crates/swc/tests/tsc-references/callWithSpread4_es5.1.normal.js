import * as swcHelpers from "@swc/helpers";
pli.apply(void 0, [
    reads
].concat(swcHelpers.toConsumableArray(gun), [
    tr,
    fun
], swcHelpers.toConsumableArray(gz), [
    writes
]));
test.apply(void 0, swcHelpers.toConsumableArray(anys));
pli.apply(void 0, [
    reads,
    writes,
    writes
]);
