import _to_consumable_array from "@swc/helpers/lib/_to_consumable_array.js";
pli.apply(void 0, [
    reads
].concat(_to_consumable_array(gun), [
    tr,
    fun
], _to_consumable_array(gz), [
    writes
]));
test.apply(void 0, _to_consumable_array(anys));
pli.apply(void 0, [
    reads,
    writes,
    writes
]);
