import(getSpecifier());
var p1 = import(getSpecifier());
import(whatToLoad ? getSpecifier() : "defaulPath"), p1.then((zero)=>zero.foo()), import([
    "path1",
    "path2"
]), import(()=>"PathToModule");
