
  x Use the isNaN function to compare with NaN
   ,----
 2 | if (foo == NaN) {}
   :     ^^^^^^^^^^
   `----

  x Use the isNaN function to compare with NaN
   ,----
 4 | if (NaN == foo) {}
   :     ^^^^^^^^^^
   `----

  x Use the isNaN function to compare with NaN
   ,----
 6 | if (foo != NaN) {}
   :     ^^^^^^^^^^
   `----

  x Use the isNaN function to compare with NaN
   ,----
 8 | if (foo == Number.NaN) {}
   :     ^^^^^^^^^^^^^^^^^
   `----

  x Use the isNaN function to compare with NaN
    ,----
 10 | if (foo != Number.NaN) {}
    :     ^^^^^^^^^^^^^^^^^
    `----

  x Use the isNaN function to compare with NaN
    ,----
 12 | if (foo == Number['NaN']) {}
    :     ^^^^^^^^^^^^^^^^^^^^
    `----

  x Use the isNaN function to compare with NaN
    ,----
 21 | myArray.indexOf(NaN);
    : ^^^^^^^^^^^^^^^^^^^^
    `----

  x Use the isNaN function to compare with NaN
    ,----
 23 | myArray.lastIndexOf(NaN);
    : ^^^^^^^^^^^^^^^^^^^^^^^^
    `----

  x Use the isNaN function to compare with NaN
    ,----
 25 | myArray.lastIndexOf(Number.NaN);
    : ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
    `----

  x Use the isNaN function to compare with NaN
    ,----
 27 | myArray.lastIndexOf(Number['NaN']);
    : ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
    `----
