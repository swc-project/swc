
  x the name `foo` is defined multiple times
   ,-[3:1]
 3 | } catch (foo) {
   :          ^|^
   :           `-- previous definition of `foo` here
 4 |     let foo;
   :         ^|^
   :          `-- `foo` redefined here
   `----
