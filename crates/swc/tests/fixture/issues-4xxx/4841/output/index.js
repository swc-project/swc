global._processChunk = function r(r, c, i) {
    if (r) {
        if (!c) {
            return true;
        }
        var i = this.write();
        return (i.callback = callback), void 0;
    }
    cb();
};
