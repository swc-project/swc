const render = () => {
    return <widget.component.type />;
};

const foo = () => {
    return (
        <widget.string>
            <Foo.interface />
        </widget.string>
    );
};

const bar = () => {
    return <widget:number></widget:number>;
};
