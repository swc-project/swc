This fixture test when the globals are directly in the reduced format.

Example:

```json
// not reduced      ----->    // reduced
./phenomena.levels            phenomenaLevels
./some/index.js               someIndexJs
.                             _
```