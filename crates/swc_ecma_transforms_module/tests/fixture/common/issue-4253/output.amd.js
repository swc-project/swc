define([
    "require"
], function(require) {
    "use strict";
    let pipeline = await new Promise((resolve1, reject)=>require([
            await resolve(file)
        ], (m)=>resolve1(/*#__PURE__*/ _interopRequireWildcard(m)), reject));
});
