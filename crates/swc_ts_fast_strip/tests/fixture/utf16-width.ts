const runtimeBefore = "前😀";

type Removed =
    | "字"
    | "😀";

interface Shape {
    value: Removed;
}
[1].forEach(() => {});

const value: Removed = "字" as Removed;
type 𐐀 = number;
const arrow = ():
    𐐀 =>
    1;
const runtimeAfter: "後😀" = "後😀"; throw new Error("utf16-position");

void [runtimeBefore, value, arrow, runtimeAfter];
