async function o(o) {
    for await (a of o) {
    }
    for await (var f of o) {
    }
    for await (let a of o) {
    }
    for await (const a of o) {
    }
}
const f = async (o) => {
    for await (a of o) {
    }
    for await (var f of o) {
    }
    for await (let a of o) {
    }
    for await (const a of o) {
    }
};
