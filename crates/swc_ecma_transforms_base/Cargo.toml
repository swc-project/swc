[package]
authors       = ["강동윤 <kdy1997.dev@gmail.com>"]
description   = "rust port of babel and closure compiler."
documentation = "https://rustdoc.swc.rs/swc_ecma_transforms_base/"
edition       = { workspace = true }
include       = ["Cargo.toml", "src/**/*.rs", "src/**/*.js"]
license       = { workspace = true }
name          = "swc_ecma_transforms_base"
repository    = { workspace = true }
version       = "36.0.1"

[lib]
bench = false

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(swc_ast_unknown)'] }

[features]
concurrent         = ["concurrent-renamer", "par-iter", "swc_ecma_utils/concurrent"]
concurrent-renamer = ["par-iter"]
# Allow helpers to be inlined. If this feature is disabled, the helpers will always be imported.
inline-helpers = []

[dependencies]
better_scoped_tls = { version = "1.0.1", path = "../better_scoped_tls", default-features = false }
indexmap = { workspace = true, default-features = false }
once_cell = { workspace = true, default-features = false }
par-core = { workspace = true, default-features = false }
par-iter = { workspace = true, optional = true, default-features = false }
phf = { workspace = true, features = ["macros"], default-features = false }
rustc-hash = { workspace = true, default-features = false }
serde = { workspace = true, features = ["derive"], default-features = false }
tracing = { workspace = true, default-features = false }

swc_atoms = { version = "9.0.0", path = "../swc_atoms", default-features = false }
swc_common = { version = "18.0.1", path = "../swc_common", default-features = false }
swc_ecma_ast = { version = "20.0.1", path = "../swc_ecma_ast", default-features = false }
swc_ecma_parser = { version = "33.0.1", path = "../swc_ecma_parser", default-features = false, features = [
  "typescript",
] }
swc_ecma_utils = { version = "26.0.1", path = "../swc_ecma_utils", default-features = false }
swc_ecma_visit = { version = "20.0.0", path = "../swc_ecma_visit", default-features = false }

[dev-dependencies]
codspeed-criterion-compat = { workspace = true }
par-core = { workspace = true, features = ["chili"], default-features = false }

swc_ecma_codegen = { version = "23.0.0", path = "../swc_ecma_codegen", default-features = false }
swc_ecma_transforms_macros = { version = "1.0.1", path = "../swc_ecma_transforms_macros", default-features = false }
swc_malloc = { version = "1.2.4", path = "../swc_malloc", default-features = false }
testing = { version = "19.0.0", path = "../testing", default-features = false }

[[bench]]
harness = false
name    = "base"
