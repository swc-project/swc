const enum A {
    A,
}

export enum B {
    B,
}

{
    const enum C {
        C,
    }
}

label: const enum D {
    D,
}

if (Math.random())
    const enum E {
        E,
    }
else
    const enum F {
        F,
    }

while (Math.random())
    const enum G {
        G,
    }

do
    const enum H {
        H,
    }
while (Math.random());

for (const _ of Math.random())
    const enum I {
        I,
    }

switch (Math.random()) {
    case 1:
        const enum J {
            J,
        }
}

;
