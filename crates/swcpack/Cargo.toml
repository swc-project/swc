[package]
edition = "2021"
name = "swcpack"
publish = false
version = "0.1.0"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
anyhow = "1.0.57"
async-trait = "0.1.53"
swc_common = {features = ["concurrent"], path = "../swc_common"}
swc_ecma_ast = {path = "../swc_ecma_ast"}
swc_ecma_loader = {features = ["node"], path = "../swc_ecma_loader"}
swc_ecma_parser = {path = "../swc_ecma_parser"}
swcpack_core = {path = "../swcpack_core"}
swcpack_node_interop = {path = "../swcpack_node_interop"}
tokio = "1.18.2"

[dev-dependencies]
testing = { path = "../testing" }
