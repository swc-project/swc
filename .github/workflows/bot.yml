name: SWC Bot

# on:
#   push:
#     branches:
#       - main
#   schedule:
#     - cron: "0,30 * * * *"

on: [push, pull_request]

jobs:
  auto-rebase:
    name: Auto rebase
    runs-on: ubuntu-latest
    concurrency: swc-bot-auto-rebase
    steps:
      - name: Checkout
        uses: actions/checkout@v2
